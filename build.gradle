apply plugin: 'maven-publish'

publishing {
    publications {
        aar(MavenPublication) {
            groupId 'com.mxspace'
            artifactId 'nuisdk'
            version '0.1.3'
            artifact("$projectDir/libs/nuisdk-release.aar")
        }
    }
}

