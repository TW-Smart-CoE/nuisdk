apply plugin: 'maven-publish'

publishing {
    publications {
        aar(MavenPublication) {
            groupId 'com.github.TW-Smart-CoE'
            artifactId 'nuisdk'
            version '0.1.0'
            artifact("$projectDir/libs/nuisdk-release.aar")
        }
    }
}

task assemble {}
