apply plugin: 'maven-publish'

publishing {
    publications {
        aar(MavenPublication) {
            groupId 'com.github.jie-meng'
            artifactId 'nuisdk'
            version '0.1.5'
            artifact("$projectDir/libs/nuisdk-release.aar")
        }
    }
}

task assemble {}
