apply plugin: 'maven-publish'

publishing {
    publications {
        aar(MavenPublication) {
            groupId 'com.mxspace'
            artifactId 'nuisdk'
            version '0.1.4'
            artifact("$projectDir/libs/nuisdk-release.aar")
        }
    }
}

task assemble {}
